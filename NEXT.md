<!-- SPDX-License-Identifier: LicenseRef-PolyForm-Internal-Use-1.0.0 -->
<!-- Copyright (C) 2006-2026 DIY Accounting Limited -->

# NEXT — current state & kickoff

Living handover for this repository. Rules and shape: `../NEXT.md` (DONE or OPEN only, nothing
deferred; a bug found fixing item A is A's remainder, not a new item; this file holds ONLY what
to do next — completed work lives in `git log`). Plans of record: `PLAN_*.md` at this root.

## Open items

Items marked (Bn) are backlog rows in `BACKLOG.md`, which carries each one's full value
reasoning. Every item ends with a tag line: **Source** (backlog row, GitHub issue, plan doc, or
none), **Owner** (`Operator` for steps a workflow cannot do, `Claude Code` for steps a sub-agent
runs), and for Claude Code steps the **Model** a sub-agent should use (Fable > Opus > Sonnet >
Haiku; the lowest tier that fits). Anything touching code goes through a `claude/*` branch and
PR; the operator merges.

**Prod runs deployment prod-cceeba8** (PR #368's merge deploy, run 36238939555, 2026-09-26; the confirmation statement activity is listed on ci only).
**ci**: `ci-set2` is last-known-good (built 2026-09-25 20:17 UTC by PR #363's deploy) and the only ci set standing.
No pull request is open in this repository or its siblings.

The board runs in five sections, in this order: **in flight** (a branch, a pull request or a run
in motion, each named in the row), **machine-only**, **machine-ask**, **human-driven**,
**blocked**. The section is the classification — what it takes to carry the row to
completion, not who owns it now — so no row carries a separate tag that could drift from where it
sits. `machine-ask` is work a session drives end to end with a human present to authenticate or
approve: a second factor, an SSO login, a command the session's policy denies, a send from the
operator's address, a write to Google or GitHub the operator says go to. `human-driven` is work a
human must navigate themselves: a coding assistant's practical limits, a physical restriction
beyond one authentication (a proctored exam, a signature in person), or policy (a payment mandate,
a filing against the operator's own company, a decision between named alternatives). A row whose
only human step is merging its PR is machine-only; that is the standing workflow, not an action
the row needs. Within a section, items run by the size of the change to committed files, least
first (operator, 2026-09-13); a row that changes nothing committed — a comment, a run, a scan, a
console action — comes before any code. Operator items are briefed in
`../NEXT_OPERATOR_RUNBOOK.md` at the workspace root, one file rewritten in place. Every item
names its model: the lowest tier that fits (Fable > Opus > Sonnet > Haiku), or `none` for a human
step.

## In flight

## Machine-only

- [ ] **CS-A2. Companies House test-service harness.** Builds what `PLAN_COMPANIES_HOUSE.md`'s assumed authorisation criteria (rows 1 to 7 and 9) need. Where to test is settled (probes of 2026-09-26, exchanges in `../staging/xmlgw-where-to-test/`): the test service `https://xmlgw.companieshouse.gov.uk/v1-0/xmlgw/Gateway` with `GatewayTest` 1, package reference 0012 and the test presenter (ci secrets `ci/submit/companies-house/presenter_id` and `presenter_code`, the values `.env.ci` lines 56 to 57 name); its `/SchemaStatus` lists `ConfirmationAndVerificationStatement-v1-0.xsd`, `PSCVerificationStatement-v1-0.xsd` and `CompanyData-v3-6.xsd`. The live presenter is refused there (502); the sandpit staging host validates schemas only. `CompanyDataRequest` answers only the test companies 00001350, 04549236, 06060501, 03950344, 04615520 and 01966794. Write `scripts/companies-house-test-service-run.js` over `app/services/companiesHouseXmlGateway.js`'s exports (`buildCompanyDataRequest` line ~328, `buildPaymentPeriodsRequest` ~364, `buildConfirmationStatementSubmission` ~281, `buildStatusRequest` ~395, `parseGatewayResponse` ~429, `allocateSubmissionNumber` ~643, `redactPresenterCredentials` ~672, `postToGateway` ~699) and the poll loop in `scripts/companies-house-xmlgw-poll.js`, duplicating neither. Cases in one fixture file, each with its expected outcome: a `CompanyDataRequest`, a `PaymentPeriodsRequest`, a no-change statement, a SIC change, one with `Shareholdings`, a registered email change, one with a blank director code, one with a wrong company authentication code, and the `ConfirmationStatement-v1-3` schema where test data allows. Poll each to `ACCEPT` or `REJECT`, honouring `PollInterval`. Write an evidence log (case, submission number, transaction id, timestamps, status, reject codes) and each exchange passed through `redactPresenterCredentials`, to an `--out-dir` outside the repository. Pin every observed response in `app/http-simulator/routes/companies-house-xmlgw.js`; map each observed reject code and `GovTalkErrors` number to plain words on `fileConfirmationStatement.html`; unit tests for the case runner and the map. Settles Q2 and Q3. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~7 files.

- [ ] **TG1. Synthetic traffic in the prod Telegram group.** `diy-prod-live` on 2026-09-26 took about 15 "[submit/prod] New session: human from US" and five "HMRC token exchanged" messages within 11:27 to 11:30 (operator's screenshots), a burst shaped like a probe or video-capture run, all labelled human. `app/functions/account/sessionBeaconPost.js` labels a session from its user agent only (`classifyVisitor`, `app/lib/visitorClassifier.js`), so a headless browser with a normal user agent reads as human; `app/functions/ops/activityTelegramForwarder.js` `resolveTargetChatIds` routes to the test chat only on a test-prefixed requestId; `app/lib/activityAlert.js` line ~119 already matches a request's Cognito sub against the synthetic lane users. Sample the last 24 hours of activity events that reached the live chat (the `activity_events` table in `prod_env_analytics`, workgroup `prod-env-analytics`, `summary`, `actor`, `detail_json`, `request_id`, `app_client`), line them up with the probe (`probe-test.yml`), deploy behaviour-test and `video-capture*.yml` runs in the same minutes, and name each synthetic source's tell (signed-in synthetic sub, a header or query flag the runners can send, source IP ranges, the synthetic lane's user agent). The same minutes carried a CA and a BR session, so US sessions there may be real: separate by marker, not by country. Give every automated browser a detectable marker (operator, 2026-09-26): the video-capture scene runner (`site-video-capture` skill, `videos/*.json`), the probe and behaviour-test runs send it (a user-agent suffix or a request header the session beacon and the API read), and the beacon and activity publishers tag those sessions synthetic so they go to the test chat and the lake's synthetic actor, with unit tests. **Source**: operator, 2026-09-26. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~4 files.

- [ ] **TG2. Prod Telegram: fewer, more useful messages at today's traffic.** Runs after TG1 in the same agent (both read the same activity events and change the same forwarder). After TG1 strips synthetic traffic, measure what is left over the last 7 days by message type (sessions, token exchanges, logins, submissions, sign-ups, errors), and for each "New session" whether it became a sign-in, an authenticated session, or stayed a visitor, and how many are crawlers the user-agent check misses (no JS beacon from real crawlers; check against CloudFront logs in the lake). The operator wants to know about new users. Recommend a message set sized to the real volume: e.g. drop per-visitor "New session" lines for a daily digest, keep an immediate line for a new account, a first submission, a payment and a failure, and put counts on the dashboard; write the recommendation as the next row with its files. **Source**: operator, 2026-09-26. **Owner**: Claude Code. **Model**: Sonnet. **Size**: 0 files.

- [ ] **B30a/30a. Re-run the alarm audit, ci included, and read the CloudWatch cost anomaly with it.** BACKLOG 30a, due since 2026-09-13: re-run `../developers/submit/ALARM_AUDIT_2026-09.md`'s counts (alarm histories from the CloudWatch API, deploy runs from the GitHub Actions API) over the last seven days, per family, against its 90-day baseline, for prod and for ci. Fold in the AWS Cost Anomaly Detection alert of 2026-09-25 (monitor `org-service-spend`, reported to the management account 887764105431 because it watches the organisation): AmazonCloudWatch in submit-ci (367191799875), eu-west-2, $15.97 over 2026-09-22 to 24, max $5.97 a day, from `EUW2-CW:AlarmMonitorUsage` ($2.93) and `EUW2-CW:MetricMonitorUsage` ($2.05); ci deployed heavily again on 2026-09-25 and 26. Measure read-only: Cost Explorer daily CloudWatch cost by usage type for submit-ci since 2026-09-01 (`aws --profile management ce get-cost-and-usage`, LINKED_ACCOUNT filter), the alarm count per ci set (`aws --profile submit-ci cloudwatch describe-alarms` grouped by `ci-setN-app-`; prod holds about 660 alarms), and alarms or custom metrics left behind by sets no longer standing. Output: the per-family table and the cut list B30 acts on. **Source**: BACKLOG 30a; AWS anomaly email 2026-09-25. **Owner**: Claude Code. **Model**: Sonnet. **Size**: 0 files.

- [ ] **CS-11a. The confirmation statement's fee mode reachable when deployed.** `app/functions/companies-house/companiesHouseConfirmationStatementPost.js` line ~244 skips the fee gate when `COMPANIES_HOUSE_CS_FEE_MODE` is `operator`, but no CDK prop sets it, so operator mode cannot be reached on a deployed set. Add it as a prop through `CompaniesHouseStack.java` onto that Lambda's environment, set only where the environment's config names it (`.env.prod` for the operator's own company), with a CDK assertion that ci carries no value; `LambdaEnvironmentWiringCdkResourceTest` keeps it honest. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code. **Model**: Haiku. **Size**: ~2 files.

- [ ] **SEC1. Credential probing against prod.** On 2026-09-26 at 07:52 UTC, 45.138.12.24 (LT) requested about 20 paths against `prod-a8703b3`: `/.env` and variants (`.bak`, `.save`, `.sample`, `.example`, `.development`, `.staging`, `.openai`, `~`), `/.aws/credentials`, `/.aws/config`, `/.git/config`, `/.git/index`, `/.git/ORIG_HEAD`, `/info.php`, each reported to `diy-prod-ops` as "Scan blocked" (`app/functions/security/wafScanDetect.js`, `EdgeStack.java` line ~709). Settle whether it is ours: list the repository's scanners (`grep -l -i -E 'zap|dast|nuclei|scan' .github/workflows/*.yml`, the video-capture and probe runs) and their runner addresses (GitHub-hosted runners use Azure ranges), and whether any ran at 07:52. Confirm from the CloudFront access logs in the lake (`cloudfront_requests` in `prod_env_analytics`) that none of those paths answered 200 in the last 30 days from any source, that the WAF blocked rather than the origin 404ed, and that the web bucket holds no such files. Then propose one Telegram message per source address per burst ("45.138.12.24 (LT): 20 sensitive paths blocked in 1 min") in place of one per request; this overlaps TG2. **Source**: operator, 2026-09-26. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~2 files.

- [ ] **DB2. Last 1 hour, 1 day and 7 days columns on the Activities table.** Add three columns, no trend, before "Last 30 days" for Started and Completed on the operator dashboard's Activities table (`web/public/operator/dashboard.html`, `renderActivities`), fed by `operatorSnapshotPublish.js`'s windowed SQL. First answer for the operator: the snapshot is published nightly at 03:15 UTC, so a 1-hour or 1-day figure is up to a day stale unless the publish runs more often; state the staleness, the per-run Athena bytes scanned (from the query statistics) and the cost of an hourly run of only the activity queries (Athena $5 per TB scanned plus Lambda time; the full run takes about 316 s), and whether `activity_events`' partitioning (`dt`) makes a 1-hour window accurate. Operator's decision (2026-09-26): publish the activity figures hourly, and every 15 minutes if the measured cost stays within $1 to $2 a month; state the measured figure for both cadences before choosing. **Source**: operator, 2026-09-26. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~3 files.

- [ ] **DB1. Test and synthetic traffic in the operator dashboard's figures.** Figures on `https://submit.diyaccounting.co.uk/operator/dashboard.html` (2026-09-26) that look inflated or wrong: "Subscription cancellations" 319 and "Sessions, all channels" 319 for the same 30 days; "New accounts" 339; "Passes redeemed" 1534 against "Passes issued" 35; "Completions, all activities" 160 while the Activities table's completed column sums to 160 from bundles 31, Submit VAT 28, VAT Obligations 85, VAT Penalties 5, View VAT Return 9, registered email 2; "Returning submitters" 0; the human, bot and synthetic session rows blank. For each observation in `app/functions/analytics/operatorSnapshotPublish.js`, find the view it reads (`infra/main/java/co/uk/diyaccounting/submit/stacks/analytics/BusinessViews.java`) and whether that view excludes the synthetic actor and the test lanes (the synthetic lane users, test-prefixed request ids, ci-origin events); query each with and without the exclusion in `prod_env_analytics` to size the gap; fix the views or observations that mix them in, with tests. Uses TG1's synthetic tells. **Source**: operator, 2026-09-26. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~4 files.

- [ ] **CS-13a. PSC verification statement (VS01): the build.** A builder over `PSCVerificationStatement-v1-0.xsd` (its fixture landed with CS-1), a submit and poll Lambda pair, a simulator class, tests, and a result-view section for each director who is also a PSC, filed after the statement inside the window starting the day after the review date (the report's V4). **Source**: `PLAN_COMPANIES_HOUSE.md` (its Tasks table carries the files). **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~8 files.

## Machine-ask

## Human-driven

- [ ] **O11. The ITSA send day.** The proof is on `main` (PR #352, 2026-09-25): the eight ITSA suites pass on the simulator and run in CI, and the sandbox year ran clean on 2023-24, 2025-26 and 2026-27 on 2026-09-25 with a real `Gov-Client-Multi-Factor` header (`VALID_HEADERS`, no warnings), inside HMRC's 14 days until 2026-10-09. Send `_developers/hmrc/DRAFT_EMAIL_ITSA_RECOGNITION.md` to `SDSTeam@hmrc.gov.uk` from the operator's address (prod-23d9a7e carries PR #352), then `_developers/hmrc/DRAFT_EMAIL_ITSA_PRODUCTION_CREDENTIALS.md` when SDST answers. **Source**: BACKLOG 11; `PLAN_ITSA_PHASE_2.md` T10. **Owner**: Operator. **Model**: none. **Size**: 0 files.

## Blocked

- [ ] **CS-A3. The harness on a schedule.** `.github/workflows/companies-house-test-service.yml`: CS-A2's harness weekly (a cron off the top of the hour, like `fraud-header-check.yml`) and on `workflow_dispatch`, against the test service with the ci test presenter. It assumes the ci role the way `probe-test.yml` line ~268 does (`vars.SUBMIT_ACTIONS_ROLE_ARN`), reads the presenter secrets and the async requests table name (`COMPANIES_HOUSE_ACCOUNTS_ASYNC_REQUESTS_TABLE_NAME`, for the shared submission counter) from ci, uploads the evidence log and the redacted exchanges as run artefacts kept 90 days (Companies House keeps a result 90 days), writes the case table to the step summary, and fails the run when a case misses its expected outcome. No ITSA sandbox workflow exists to copy; `scripts/itsa-sandbox-year.js` runs locally. A weekly run keeps one inside the 14 days criterion 8 assumes. Blocked on CS-A2. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~2 files.

- [ ] **CS-A4. Software authorisation: send the evidence.** Claude Code assembles the pack from a CS-A3 run inside the last 14 days, against `PLAN_COMPANIES_HOUSE.md`'s criteria table: product and forms, one line per case with its submission number and outcome, the run link, the page's axe result (`scripts/axe-quickscan.mjs`), a declaration screenshot, contact. It drafts the email at the workspace root. The operator sends it to `xml@companieshouse.gov.uk` from their own address and asks for the live package reference for the confirmation statement (or confirmation that the accounts reference from O34c covers it), then sets it on GitHub's `prod` environment. Blocked on CS-A3. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code (the pack), Operator (the send). **Model**: Sonnet. **Size**: 0 files.

- [ ] **B30/30. Cut the alarms and canary runs that are dead weight.** BACKLOG 30: act on B30a's cut list (families that never fired or only fired as noise, per-Lambda alarms on ci sets that nobody reads, canary runs that duplicate the probes), following `../developers/submit/archive/PLAN_ALARM_CONSOLIDATION.md`; the largest recurring CloudWatch line after the composite consolidation. Blocked on B30a. **Source**: BACKLOG 30. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~4 files.

- [ ] **CS-13b. PSC verification statement (VS01): the test-service proof.** Add the PSC verification statement's cases to CS-A2's harness fixture (one director-PSC per test company that has one, plus a blank-code case); CS-A3 then runs them with the rest. Blocked on CS-13a and CS-A2. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~2 files.

- [ ] **CS-11b. Confirmation statement's customer prod launch.** DIY Accounting Limited already files its own statements through Submit under its credit-account presenter; CS-11b takes the same activity to customers on prod. `file-confirmation-statement` already lists `bundles = ["resident", "resident-pro"]`, so no operator-only gate is needed — add `prod` to its `environments` (`web/public/submit.catalogue.toml` lines 437 to 444; the VS01 activity too, when CS-13a adds one), and a live Stripe price alongside the existing one for the £61.35 fee. Prove it first: the prod gateway values and the live package reference from CS-A4, then one fee-free operator proof filing (a second statement for 06846849 in the 2026-27 payment period, operator fee mode CS-11a; the operator approved it; it moves the next review date to about a year after the filing day) before the listing goes live. Then `compliance.toml` rows for the credit account and the authorisation. Shares BACKLOG 34c steps 3 and 4 with the accounts launch. Blocked on CS-A4 and CS-11a. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~7 files.

- [ ] **CS-P1. Filing under a customer's own presenter.** A customer can give their own Companies House presenter id and authentication code instead of Submit's; Submit presents under their presenter, Companies House charges the £50 confirmation statement fee to the customer's own credit account, and Submit skips its £61.35 fee. Outside ACSP (`PLAN_COMPANIES_HOUSE_ACSP.md`), since Submit is not the one paying or engaging Companies House. Build: the page option on `web/public/companies-house/fileConfirmationStatement.html` (credentials entered per filing, never stored, with the credit-account requirement explained); `PaymentPeriodsRequest` still decides whether a fee is due; the Stripe checkout skipped at the same fee gate operator mode already uses (`app/functions/companies-house/companiesHouseConfirmationStatementPost.js` line ~244); the simulator route and its tests. Micro-entity accounts carry no fee, so the option there only changes whose presenter shows on the filing. Blocked on CS-11b, since it adds a second payment path to the journey CS-11b launches. **Source**: `PLAN_COMPANIES_HOUSE.md`. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~5 files.

- [ ] **B34.6c. Companies House accounts filing: the sandbox proof.** The XML team was asked on 2026-09-23 22:22 UTC in a new thread (from antony@, subject "Submission 000004 status and
  GetSubmissionStatus query") whether 000004 was accepted and whether lookups are enabled for test
  presenter 66666727000. When the answer says they are: poll 000004 through `GET /api/v1/companies-house/accounts/000004` on a
  standing ci set and pin the returned `StatusCode` and any rejections as a case in
  `app/unit-tests/functions/companiesHouseAccountsGet.test.js`. The prod catalogue listing is
  BACKLOG 34c's: prod carries no `COMPANIES_HOUSE_XMLGW_URI` and no presenter secret ARNs. Blocked
  on IT at Companies House: the XML team answered on 2026-09-25 13:59 that the test presenter's account "was not set up successfully, causing the error", and will reply when IT answers. They asked for the request and response on 2026-09-24; the reply went the same day with transactions 1790285530232 (9999) and 1790285532345 (502), masked (`../DRAFT_EMAIL_XMLGW_000004_REPLY.md`; the unmasked set, from the 21:42 run, is `../DRAFT_EMAIL_XMLGW_000004_REPLY_UNMASKED.md`). In the 9999 response the gateway echoes `Method` CHMD5 with an empty `Value`. **Source**: BACKLOG 34b. **Owner**: Claude Code. **Model**: Sonnet. **Size**:
  ~1 file.

- [ ] **O34c. Companies House clears the presenter for live accounts filing.** BACKLOG 34c steps 2 and 3, after B34.6c's sandbox proof: ask the XML team (`xml@companieshouse.gov.uk`) to clear the new presenter that holds the credit account (its id in `../NEXT_OPERATOR_RUNBOOK.md` task A; it authenticates on the live gateway since 2026-09-26) for the live service and issue the live package reference (the test one is 0012); then set the live presenter id, presenter code and `COMPANIES_HOUSE_PACKAGE_REFERENCE` on GitHub's `prod` environment (Settings, Environments, prod), which `deploy-environment.yml` carries into Secrets Manager. Blocked on B34.6c. **Source**: BACKLOG 34c. **Owner**: Operator. **Model**: none. **Size**: 0 files.

- [ ] **B34c. Companies House accounts filing launched on prod.** BACKLOG 34c steps 4 to 6: `CompaniesHouseStack.java` sets the prod values (`COMPANIES_HOUSE_GATEWAY_TEST=false`, the live package reference) instead of leaving them unset; one filing on the prod lane for a company the operator controls, polled to a terminal state; `prod` added to `file-micro-entity-accounts`' `environments` and `resident`'s listing in `web/public/submit.catalogue.toml`, with the activity page and the accounts video no longer calling it a sandbox preview. Blocked on B34.6c and O34c. **Source**: BACKLOG 34c. **Owner**: Claude Code. **Model**: Sonnet. **Size**: ~5 files.

- [ ] **F-BS1. The 2026-27 company book's negative balance-sheet lines.** Cowork's check of `../staging/2026-2027/book/` (diya-gl 1.2.34, 2026-09-24): every check passes, but the published balance sheet shows trade debtors −£425.36 and trade creditors −£681.52, and net assets £1,337.3033 against shareholders' funds £1,337.3233 (2p). Causes it named: `BANK-2026-04-13-15-SUSPENSE` £527.41 (bank code DR; the 13 April "POLYCODE LIMITED, DL REPAYMENT" of the same amount is the likely pair); six "PAYPAL PAYMENT 5JX22222WZGH6" top-ups (£184.64) coded CR with no purchases line, which are transfers into the PayPal wallet; ICO ZB070902 £47.00 on 22 May with no purchases line although the label map (`../staging/labels/diya-labels.toml`) has an ICO rule; "Matthew Grundy, DIY ACCOUNTING" £70.00 on 22 April. Resolve against the operator's own reference set of accounts, line by line, then rebuild and verify with the `company-book` skill. Blocked on the operator's reference set of accounts. **Owner**: Claude Code. **Model**: Sonnet. **Size**: 0 files (the book and the label map are under `../staging/`, a label-rule or parser fix is ~2 files).

## Discipline

- **Push once per batch of landed tracks, never per track**, and prefer one dispatch that
  proves several things over several dispatches. A push per track turned one batch into six
  ci deploys and several environment deploys in a morning on 2026-09-06, each able to open
  alarm issues and cancel each other through the deploy concurrency group, and the operator
  froze pushes twice. A freeze, when the operator calls one, stops `git push`,
  `gh workflow run` and `gh pr create` until they lift it in their own words; local commits,
  worktree tracks and reading logs continue, and a failed job gets a proposed fix in the reply.

