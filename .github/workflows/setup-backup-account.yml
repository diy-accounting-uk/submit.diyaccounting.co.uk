name: setup backup account
run-name: "Setup backup account ${{ inputs.backup-account-id }}"

# Manual workflow that provisions the submit-backup account with the vault that receives
# backup copies from the deployment accounts.
#
# The backup account holds nothing else. Keeping it separate means losing a deployment
# account does not lose the backups, which only holds if nothing in a deployment account
# can reach in and delete them - hence the vault access policy in the stack.
#
# Prerequisites:
# - The backup account exists and has OIDC trust for GitHub Actions
# - IAM role backup-github-actions-role exists there, with permissions for KMS, AWS Backup and CloudFormation
# - The account is CDK-bootstrapped in the primary region
#
# See _developers/backlog/PLAN_CROSS_ACCOUNT_BACKUPS.md section 6 for the full spec.

on:
  workflow_dispatch:
    inputs:
      backup-account-id:
        description: 'AWS Account ID for the dedicated backup account'
        type: string
        required: true
        default: '914216784828'
      primary-region:
        description: 'Region holding the cross-account vault'
        type: string
        required: true
        default: 'eu-west-2'
      dry-run:
        description: 'Dry run (cdk diff only, no changes applied)'
        type: boolean
        required: false
        default: true

env:
  JAVA_VERSION: '21'
  BACKUP_ACTIONS_ROLE_ARN: 'arn:aws:iam::${{ inputs.backup-account-id }}:role/backup-github-actions-role'

jobs:
  setup:
    name: 'Setup backup account'
    runs-on: ubuntu-24.04
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Validate inputs
        run: |
          if ! echo "${{ inputs.backup-account-id }}" | grep -qE '^[0-9]{12}$'; then
            echo "ERROR: Invalid AWS Account ID format. Expected 12 digits."
            exit 1
          fi
          echo "Backup account: ${{ inputs.backup-account-id }}"
          echo "Region:         ${{ inputs.primary-region }}"
          echo "Dry run:        ${{ inputs.dry-run }}"

      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install npm dependencies
        run: npm ci
        timeout-minutes: 6

      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'maven'

      - name: Configure AWS credentials (backup account)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.BACKUP_ACTIONS_ROLE_ARN }}
          aws-region: ${{ inputs.primary-region }}
          role-chaining: false
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          retry-max-attempts: 3

      - name: Build backup CDK app
        run: |
          ./mvnw --errors clean verify -DskipTests -P cdk-backup \
            -Dmaven.compiler.source=${{ env.JAVA_VERSION }} \
            -Dmaven.compiler.target=${{ env.JAVA_VERSION }}

      - name: Show planned changes (cdk diff)
        if: inputs.dry-run == true
        working-directory: cdk-backup
        run: |
          npx cdk diff backup-CrossAccountBackupVaultStack --no-notices
          echo ""
          echo "Dry run only. Re-run with dry-run unchecked to apply."

      - name: Deploy cross-account backup vault
        if: inputs.dry-run == false
        working-directory: cdk-backup
        run: |
          npx cdk deploy backup-CrossAccountBackupVaultStack \
            --require-approval never \
            --ci true \
            --no-notices \
            --outputs-file ../cdk-submit-backup.out/cdk-outputs-backup.json

      - name: Report vault details
        if: inputs.dry-run == false
        run: |
          jq -r '.["backup-CrossAccountBackupVaultStack"]' cdk-submit-backup.out/cdk-outputs-backup.json
          echo ""
          echo "Next: set these on the deployment accounts so BackupStack can copy into the vault."
          echo "See PLAN_CROSS_ACCOUNT_BACKUPS.md section 7 (Phase 3.2)."
